<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.main.mj_write.PostMapper">

	<select id="showAllPostList" resultType="com.project.main.mj_write.Post">
		select * from post
	</select>
	
	<select id="detailPost" resultType="com.project.main.mj_write.Post">
		select * from post where postNum = #{postNum}
	</select>

</mapper>